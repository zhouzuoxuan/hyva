<?php
declare(strict_types=1);

/** @var \Magento\Framework\View\Element\Template $block */
?>
<section class="pt-10">
    <div class="flex items-center justify-between border-b border-slate-200 pb-4">
        <h2 class="text-[clamp(1.7rem,2.4vw,2.5rem)] font-semibold tracking-tight text-slate-900">
            Shipping Address
        </h2>

        <template x-if="shippingSaveState === 'saving'">
            <span class="text-xs font-semibold text-amber-700">Saving…</span>
        </template>
        <template x-if="shippingSaveState === 'saved'">
            <span class="text-xs font-semibold text-emerald-700">Saved</span>
        </template>
        <template x-if="shippingSaveState === 'error'">
            <span class="text-xs font-semibold text-rose-700">Please review</span>
        </template>
    </div>

    <template x-if="showAddressIncompleteNotice()">
        <div class="mt-4 rounded-md border border-amber-300 bg-amber-50 px-4 py-3 text-sm text-amber-800"
             x-text="getAddressIncompleteMessage()"></div>
    </template>

    <div class="grid gap-5 pt-6 sm:grid-cols-2">
        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                First name <span x-show="isAddressFieldRequired('firstname')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.firstname"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Enter first name"
            >
        </label>

        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Last name <span x-show="isAddressFieldRequired('lastname')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.lastname"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Enter last name"
            >
        </label>

        <label class="block sm:col-span-2">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Company <span x-show="isAddressFieldRequired('company')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.company"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Company"
            >
        </label>

        <label class="block sm:col-span-2">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Street address <span x-show="isAddressFieldRequired('street_1')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.street_1"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Enter street address 1"
            >
        </label>

        <label class="block sm:col-span-2">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Address line 2 <span x-show="isAddressFieldRequired('street_2')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.street_2"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Enter street address 2"
            >
        </label>

        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Country <span x-show="isAddressFieldRequired('country_id')" class="text-rose-600">*</span>
            </span>
            <select
                x-model="shipping.country_id"
                @change="queueShippingAutosave('country')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition focus:border-slate-900"
            >
                <template x-for="option in (config.countryOptions || [])" :key="option.value">
                    <option :value="option.value" x-text="option.label"></option>
                </template>
            </select>
        </label>

        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                County / Region <span x-show="isAddressFieldRequired('region')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.region"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="County / Region"
            >
        </label>

        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                City <span x-show="isAddressFieldRequired('city')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.city"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="City"
            >
        </label>

        <label class="block">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Postcode <span x-show="isAddressFieldRequired('postcode')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.postcode"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Postcode"
            >
        </label>

        <label class="block sm:col-span-2">
            <span class="mb-1.5 block text-xs font-medium text-slate-700">
                Phone number <span x-show="isAddressFieldRequired('telephone')" class="text-rose-600">*</span>
            </span>
            <input
                x-model="shipping.telephone"
                @input="markShippingDirty()"
                @change="queueShippingAutosave('field')"
                @blur="queueShippingAutosave('field')"
                class="w-full border border-slate-300 bg-white px-3 py-3 text-base outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                placeholder="Phone number"
            >
        </label>
    </div>
</section>
