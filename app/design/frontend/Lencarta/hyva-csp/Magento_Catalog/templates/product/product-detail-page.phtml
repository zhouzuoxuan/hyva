<?php
/**
 * Hyvä Themes - https://hyva.io
 * Copyright © Hyvä Themes. All rights reserved.
 * See COPYING.txt for license details.
 */

declare(strict_types=1);

use Magento\Catalog\Block\Product\View;
use Magento\Framework\Escaper;

/** @var View $block */
/** @var Escaper $escaper */
?>

<section
    class="pb-6 md:py-6 grid grid-cols-1 gap-y-4
           md:items-start md:grid-cols-2 md:gap-x-2
           lg:grid-cols-[11fr_9fr] lg:gap-x-4"
    aria-label="<?= $escaper->escapeHtmlAttr(__('Product Info')) ?>"
>
    <!-- Left: sticky only on desktop, no fixed height, no overflow -->
    <div class="min-w-0 lg:sticky lg:top-24 lg:self-start">
        <?= $block->getChildHtml('product.media') ?>
    </div>

    <!-- Right: normal flow (page scroll), no sticky, no inner scrollbar -->
    <div class="min-w-0">
        <?= $block->getChildHtml('product.info') ?>
    </div>
</section>

<?= $block->getChildHtml('product_options_wrapper_bottom') ?>
<?= $block->getChildHtml('product.info.details'); ?>
<?= $block->getChildHtml('related') ?>
<?= $block->getChildHtml('upsell') ?>

<section aria-label="<?= $escaper->escapeHtmlAttr(__('Product Reviews')) ?>">
    <?= $block->getChildHtml('review_list') ?>
    <?= $block->getChildHtml('review_form') ?>
</section>
