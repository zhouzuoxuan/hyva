<?php
declare(strict_types=1);

/** @var \Magento\Framework\View\Element\Template $block */

$homeUrl = $block->getUrl('');
$fallbackLogo = $block->getViewFileUrl('images/logo.svg');

$logoBlock = $block->getLayout()->createBlock(\Magento\Theme\Block\Html\Header\Logo::class);
$logoSrc = $logoBlock ? (string) $logoBlock->getLogoSrc() : '';
$logoAlt = $logoBlock ? (string) $logoBlock->getLogoAlt() : 'Logo';

if ($logoSrc === '') {
    $logoSrc = $fallbackLogo;
}
if ($logoAlt === '') {
    $logoAlt = 'Logo';
}
?>
<script>
    window.lencartaCheckoutConfig = {
        urls: {
            state: '<?= $block->escapeJs($block->getUrl('lencarta_checkout/ajax/checkoutState')) ?>',
            saveEmail: '<?= $block->escapeJs($block->getUrl('lencarta_checkout/ajax/saveEmail')) ?>',
            saveShippingAddress: '<?= $block->escapeJs($block->getUrl('lencarta_checkout/ajax/saveShippingAddress')) ?>',
            saveShippingMethod: '<?= $block->escapeJs($block->getUrl('lencarta_checkout/ajax/saveShippingMethod')) ?>',
            getTotals: '<?= $block->escapeJs($block->getUrl('lencarta_checkout/ajax/getTotals')) ?>'
        },
        homeUrl: '<?= $block->escapeJs($homeUrl) ?>'
    };
</script>

<div x-data="initLencartaCheckout(window.lencartaCheckoutConfig)"
     x-init="init()"
     class="min-h-screen bg-white text-slate-900">

    <div class="grid min-h-screen grid-cols-1 lg:grid-cols-[minmax(0,1.618fr)_minmax(360px,0.95fr)]">

        <!-- LEFT -->
        <section class="min-w-0 bg-white">
            <div class="mx-auto max-w-5xl px-8 py-8 lg:px-12 lg:py-10">

                <!-- Logo -->
                <div class="pb-10">
                    <a href="<?= $block->escapeUrl($homeUrl) ?>"
                       aria-label="Home"
                       title="Go to Home page"
                       class="inline-flex items-center">
                        <img
                            src="<?= $block->escapeUrl($logoSrc) ?>"
                            alt="<?= $block->escapeHtmlAttr($logoAlt) ?>"
                            class="h-12 w-auto"
                            loading="eager"
                            decoding="async"
                        >
                    </a>
                </div>

                <!-- Contact -->
                <section>
                    <div class="flex items-center justify-between border-b border-slate-200 pb-5">
                        <h2 class="text-[clamp(2rem,3vw,3rem)] font-semibold tracking-tight text-slate-900">
                            Contact Information
                        </h2>

                        <template x-if="emailSaveState === 'saving'">
                            <span class="text-xs font-semibold text-amber-700">Saving…</span>
                        </template>
                        <template x-if="emailSaveState === 'saved'">
                            <span class="text-xs font-semibold text-emerald-700">Saved</span>
                        </template>
                        <template x-if="emailSaveState === 'error'">
                            <span class="text-xs font-semibold text-rose-700">Check email</span>
                        </template>
                    </div>

                    <div class="pt-8">
                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">E-mail address</span>
                            <input
                                type="email"
                                x-model="email"
                                @input.debounce.700ms="autoSaveEmail()"
                                @blur="autoSaveEmail()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Enter e-mail address"
                            >
                        </label>

                        <p class="mt-4 text-sm text-slate-600">
                            You can create an account after checkout.
                        </p>
                    </div>
                </section>

                <!-- Shipping Address -->
                <section class="pt-14">
                    <div class="flex items-center justify-between border-b border-slate-200 pb-5">
                        <h2 class="text-[clamp(2rem,3vw,3rem)] font-semibold tracking-tight text-slate-900">
                            Shipping Address
                        </h2>

                        <template x-if="shippingSaveState === 'saving'">
                            <span class="text-xs font-semibold text-amber-700">Saving…</span>
                        </template>
                        <template x-if="shippingSaveState === 'saved'">
                            <span class="text-xs font-semibold text-emerald-700">Saved</span>
                        </template>
                        <template x-if="shippingSaveState === 'error'">
                            <span class="text-xs font-semibold text-rose-700">Please review</span>
                        </template>
                    </div>

                    <div class="grid gap-6 pt-8 sm:grid-cols-2">
                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">First name</span>
                            <input
                                x-model="shipping.firstname"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Enter first name"
                            >
                        </label>

                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Last name</span>
                            <input
                                x-model="shipping.lastname"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Enter last name"
                            >
                        </label>

                        <label class="block sm:col-span-2">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Company</span>
                            <input
                                x-model="shipping.company"
                                @input.debounce.900ms="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Company"
                            >
                        </label>

                        <label class="block sm:col-span-2">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Street address</span>
                            <input
                                x-model="shipping.street_1"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Enter street address 1"
                            >
                        </label>

                        <label class="block sm:col-span-2">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Address line 2</span>
                            <input
                                x-model="shipping.street_2"
                                @input.debounce.900ms="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Enter street address 2"
                            >
                        </label>

                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">City</span>
                            <input
                                x-model="shipping.city"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="City"
                            >
                        </label>

                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Postcode</span>
                            <input
                                x-model="shipping.postcode"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Postcode"
                            >
                        </label>

                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">County / Region</span>
                            <input
                                x-model="shipping.region"
                                @input.debounce.900ms="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="County / Region"
                            >
                        </label>

                        <label class="block">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Country code</span>
                            <input
                                x-model="shipping.country_id"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg uppercase outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="GB"
                            >
                        </label>

                        <label class="block sm:col-span-2">
                            <span class="mb-2 block text-sm font-medium text-slate-700">Phone number</span>
                            <input
                                x-model="shipping.telephone"
                                @input.debounce.900ms="queueShippingAutosave()"
                                @blur="queueShippingAutosave()"
                                class="w-full border border-slate-400 bg-white px-4 py-4 text-lg outline-none transition placeholder:text-slate-400 focus:border-slate-900"
                                placeholder="Phone number"
                            >
                        </label>
                    </div>
                </section>

                <!-- Delivery -->
                <section class="pt-14">
                    <div class="border-b border-slate-200 pb-5">
                        <h2 class="text-3xl font-semibold text-slate-900">Delivery Options</h2>
                    </div>

                    <div class="pt-6">
                        <template x-if="!canLoadShippingMethods()">
                            <div class="border border-slate-300 bg-white px-4 py-4 text-sm text-slate-600">
                                Enter your address to see available delivery options.
                            </div>
                        </template>

                        <template x-if="canLoadShippingMethods() && shippingMethodsState === 'loading'">
                            <div class="border border-slate-300 bg-white px-4 py-4 text-sm text-slate-600">
                                Loading delivery options…
                            </div>
                        </template>

                        <template x-if="canLoadShippingMethods() && shippingMethodsState === 'unavailable'">
                            <div class="border border-amber-300 bg-amber-50 px-4 py-4 text-sm text-amber-800">
                                No delivery options are available for this address.
                            </div>
                        </template>

                        <template x-if="shippingMethodsState === 'ready'">
                            <div class="space-y-3">
                                <template x-for="method in shippingMethods" :key="method.code">
                                    <label
                                        class="block cursor-pointer border p-4 transition"
                                        :class="selectedShippingMethod === method.code ? 'border-slate-900 bg-slate-50' : 'border-slate-300 bg-white hover:border-slate-500'"
                                    >
                                        <div class="flex items-start gap-4">
                                            <input
                                                type="radio"
                                                name="shipping_method"
                                                class="mt-1"
                                                :checked="selectedShippingMethod === method.code"
                                                @change="selectShippingMethod(method)"
                                            >
                                            <div class="min-w-0 flex-1">
                                                <div class="flex items-start justify-between gap-3">
                                                    <div>
                                                        <div class="text-sm font-semibold text-slate-900" x-text="method.carrier_title"></div>
                                                        <div class="mt-1 text-sm text-slate-500" x-text="method.method_title"></div>
                                                    </div>
                                                    <div class="whitespace-nowrap text-sm font-semibold text-slate-900" x-text="method.price"></div>
                                                </div>
                                            </div>
                                        </div>
                                    </label>
                                </template>
                            </div>
                        </template>
                    </div>
                </section>

                <!-- Payment -->
                <section class="pt-14 pb-12">
                    <div class="border-b border-slate-200 pb-5">
                        <h2 class="text-3xl font-semibold text-slate-900">Payment Method</h2>
                    </div>

                    <div class="pt-6">
                        <div class="border border-slate-300 bg-white px-4 py-4">
                            <div class="flex items-center justify-between gap-4">
                                <div>
                                    <div class="text-sm font-semibold text-slate-900">PayPal</div>
                                    <div class="mt-1 text-sm text-slate-500">Fast checkout with buyer protection and secure payment.</div>
                                </div>
                                <div class="border border-slate-300 bg-slate-50 px-3 py-1 text-xs font-semibold text-slate-600">
                                    Available
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </section>

        <!-- RIGHT -->
        <aside class="min-w-0 bg-[#f7f7f7] border-l border-slate-200">
            <div class="px-5 py-6 lg:px-6 lg:py-7 lg:sticky lg:top-0 lg:h-screen lg:overflow-hidden">

                <div class="shrink-0">
                    <h2 class="text-2xl lg:text-3xl font-semibold text-slate-900">Order Summary</h2>
                </div>

                <!-- Product list -->
                <div class="mt-8 shrink-0">
                    <div class="lencarta-products-scroll max-h-60 overflow-y-auto pr-1">
                        <div class="space-y-5">
                            <template x-for="item in visibleItems()" :key="item.item_id">
                                <div class="flex items-start gap-3">
                                    <div class="h-16 w-16 lg:h-20 lg:w-20 shrink-0 overflow-hidden border border-slate-200 bg-white">
                                        <template x-if="item.image_url">
                                            <img :src="item.image_url" :alt="item.name" class="h-full w-full object-cover">
                                        </template>
                                        <template x-if="!item.image_url">
                                            <div class="flex h-full w-full items-center justify-center text-xs text-slate-400">No image</div>
                                        </template>
                                    </div>

                                    <div class="min-w-0 flex-1">
                                        <div class="text-sm lg:text-base font-medium text-slate-900" x-text="item.name"></div>

                                        <template x-if="item.sku">
                                            <div class="mt-1 text-xs lg:text-sm text-slate-500">
                                                SKU: <span x-text="item.sku"></span>
                                            </div>
                                        </template>

                                        <template x-if="item.options_summary">
                                            <div class="mt-1 text-xs lg:text-sm text-slate-500" x-text="item.options_summary"></div>
                                        </template>

                                        <div class="mt-1 text-xs lg:text-sm text-slate-500">
                                            Qty: <span x-text="item.qty"></span>
                                        </div>

                                        <div class="mt-2 text-xl lg:text-2xl font-semibold text-slate-900" x-text="item.row_total"></div>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </div>

                    <template x-if="hasHiddenItems()">
                        <div class="mt-4">
                            <button type="button"
                                    @click="toggleItemsExpanded()"
                                    class="inline-flex items-center gap-3 text-sm font-medium text-slate-700 transition hover:text-slate-900">
                                <span x-text="itemsToggleLabel()"></span>

                                <span class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-slate-300 bg-white">
                                    <svg xmlns="http://www.w3.org/2000/svg"
                                         viewBox="0 0 20 20"
                                         fill="currentColor"
                                         class="h-3.5 w-3.5 transition-transform duration-200"
                                         :class="itemsExpanded ? 'rotate-180' : ''">
                                        <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 0 1 1.06.02L10 11.168l3.71-3.938a.75.75 0 1 1 1.08 1.04l-4.25 4.512a.75.75 0 0 1-1.08 0L5.21 8.27a.75.75 0 0 1 .02-1.06Z" clip-rule="evenodd"/>
                                    </svg>
                                </span>
                            </button>
                        </div>
                    </template>
                </div>

                <!-- Coupon -->
                <div class="mt-8 shrink-0">
                    <div class="mb-2 text-sm font-semibold text-slate-900">Discount code</div>
                    <div class="flex gap-3">
                        <input
                            type="text"
                            x-model="couponCode"
                            placeholder="Enter coupon code"
                            class="min-w-0 flex-1 border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-700 outline-none placeholder:text-slate-400 focus:border-slate-900"
                        >
                        <button type="button"
                                @click="applyCoupon()"
                                class="border border-slate-900 bg-white px-4 py-2.5 text-sm font-semibold text-slate-900 transition hover:bg-slate-900 hover:text-white">
                            Apply
                        </button>
                    </div>
                </div>

                <!-- Totals -->
                <div class="mt-6 shrink-0 border-t border-slate-200 pt-5 text-sm">
                    <div class="space-y-2">
                        <div class="flex justify-between gap-4">
                            <span class="text-slate-700">Subtotal</span>
                            <span class="font-medium text-slate-900" x-text="totals.formatted?.subtotal || '-'"></span>
                        </div>
                        <div class="flex justify-between gap-4">
                            <span class="text-slate-700">Shipping</span>
                            <span class="font-medium text-slate-900" x-text="totals.formatted?.shipping || '-'"></span>
                        </div>
                        <div class="flex justify-between gap-4">
                            <span class="text-slate-700">VAT</span>
                            <span class="font-medium text-slate-900" x-text="totals.formatted?.tax || '-'"></span>
                        </div>
                        <div class="flex justify-between gap-4">
                            <span class="text-slate-700">Discount</span>
                            <span class="font-medium text-slate-900" x-text="totals.formatted?.discount || '-'"></span>
                        </div>
                    </div>

                    <div class="mt-4 flex justify-between gap-4 text-xl lg:text-2xl font-semibold">
                        <span class="text-slate-900">Grand Total</span>
                        <span class="text-slate-900" x-text="totals.formatted?.grand_total || '-'"></span>
                    </div>
                </div>

                <!-- Terms + CTA -->
                <div class="mt-6 shrink-0">
                    <label class="mb-4 flex items-start gap-3">
                        <input type="checkbox" x-model="termsAccepted" class="mt-1 rounded border-slate-300 text-slate-900 focus:ring-0">
                        <span class="text-sm leading-6 text-slate-600">
                            I agree to the
                            <a href="#" class="font-medium text-slate-900 underline underline-offset-2">Terms &amp; Conditions</a>.
                        </span>
                    </label>

                    <div class="bg-slate-900 p-4 lg:p-5 text-white">
                        <div class="flex items-start justify-between gap-4">
                            <div>
                                <div class="text-base lg:text-lg font-semibold">Ready to pay?</div>
                                <div class="mt-1 text-xs lg:text-sm text-slate-300">
                                    Continue securely with PayPal to complete your order.
                                </div>
                            </div>
                            <div class="bg-white/10 px-2.5 py-1 text-[11px] font-semibold uppercase tracking-[0.14em] text-slate-200">
                                PayPal
                            </div>
                        </div>

                        <div class="mt-5">
                            <template x-if="!termsAccepted">
                                <div class="mb-4 border border-amber-400/30 bg-amber-400/10 px-4 py-3 text-sm text-amber-100">
                                    Please accept the terms before continuing to payment.
                                </div>
                            </template>

                            <div :class="termsAccepted ? '' : 'pointer-events-none opacity-50'">
                                <div id="lencarta-paypal-button"></div>
                            </div>
                        </div>

                        <div class="mt-3 text-[11px] text-slate-400">
                            Protected checkout. Your payment details are handled securely by PayPal.
                        </div>
                    </div>
                </div>

                <template x-if="message">
                    <div class="mt-4 shrink-0 text-sm text-slate-600" x-text="message"></div>
                </template>
            </div>
        </aside>
    </div>
</div>
