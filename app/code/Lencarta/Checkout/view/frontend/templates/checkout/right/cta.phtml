<?php
declare(strict_types=1);

/** @var \Magento\Framework\View\Element\Template $block */
?>
<div class="mt-5 shrink-0">
    <label class="mb-4 flex items-start gap-3">
        <input type="checkbox" x-model="termsAccepted" class="mt-1 rounded border-slate-300 text-slate-900 focus:ring-0">
        <span class="text-sm leading-6 text-slate-600">
            <?= $block->escapeHtml(__('I agree to the')) ?>
            <a href="#" class="font-medium text-slate-900 underline underline-offset-2"><?= $block->escapeHtml(__('Terms & Conditions')) ?></a>.
        </span>
    </label>

    <div class="rounded-xl bg-slate-900 p-4 text-white lg:p-5">
        <div class="flex items-start justify-between gap-4">
            <div>
                <div class="text-base font-semibold lg:text-lg"><?= $block->escapeHtml(__('Ready to pay?')) ?></div>
                <div class="mt-1 text-xs text-slate-300 lg:text-sm">
                    <?= $block->escapeHtml(__('Continue securely with PayPal to complete your order.')) ?>
                </div>
            </div>
            <div class="rounded-md bg-white/10 px-2.5 py-1 text-[11px] font-semibold uppercase tracking-[0.14em] text-slate-200">
                <?= $block->escapeHtml(__('PayPal')) ?>
            </div>
        </div>

        <div class="mt-5">
            <template x-if="!termsAccepted">
                <div class="mb-4 rounded-lg border border-amber-400/30 bg-amber-400/10 px-4 py-3 text-sm text-amber-100">
                    <?= $block->escapeHtml(__('Please accept the terms before continuing to payment.')) ?>
                </div>
            </template>

            <div :class="termsAccepted ? '' : 'pointer-events-none opacity-50'">
                <div id="lencarta-paypal-button"></div>
            </div>
        </div>

        <div class="mt-3 text-[11px] text-slate-400">
            Protected checkout. Your payment details are handled securely by <?= $block->escapeHtml(__('PayPal')) ?>.
        </div>
    </div>

    <template x-if="message">
        <div class="mt-4 text-sm text-slate-600" x-text="message"></div>
    </template>
</div>
